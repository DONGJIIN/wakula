Wakula 后续开发快速上手
======================

一、当前边界
----------
已完成：SLAM Toolbox、Nav2、带图像质量/IoU 多帧门控的 OpenCV 障碍证据、稳健平面与
连通域点云分类、支持乱序回调的时间同步融合、保守速度门、传感器 profile、rosbag
离线评估、强弱光/高光/模糊防御、强类型 NavigationSafety/TraversalGuidance 对接合同、
Nav2 越障入口 APPROACH/ALIGN/READY 引导、一键接口检查、
未知地图前沿探索、Nav2 入口接近、TraverseObstacle Action 编排、独立任务进程启停、Xbox
手柄安全 Twist 适配、独立 Gazebo 比赛参考场地、长时间全栈回归、测试和 CI。任务节点只
决定探索目标与越障交接；Gazebo 适配器只模拟流程，均不生成真实机器狗腿部或关节动作。

当前基线：9 个 ROS 2 包、378 项测试记录通过；2026-09-01 的三分钟确定性上层流程回归
在 48 秒完成 8/8、返回起点并进入 COMPLETED。原始传感器准确率、SLAM 回环和真机步态
必须分别验收；该成绩只代表 Gazebo 上层软件链，不代表真机越障能力。

2026-08-31 无闪现逐障碍回归：未启动仿真 TraverseObstacle/传送服务，八类障碍在各自
可观测入口视角各采 40 帧，名称、几何确认、导航健康均为 320/320；分开的动态试验 8/8
完成 Nav2 接近/对正并进入控制器等待。修复了高墙遮挡墙后激光时被地图边缘保护永久拦截
的问题，其他地图边界仍保持禁止越界。这里的 8/8 是“入口导航成功率”，不是物理跨越率；
没有腿部 /traverse_obstacle 服务时物理越障仍是 0/8，严禁据此宣称真机越障已完成。

2026-08-31 代码可读性与输入防护整理：比赛专名分类统一使用带 `_m`/`_deg` 单位的只读
量测快照，并在每个粗类型前说明匹配顺序、证据强弱和保守回退原因；删除同一字段的重复
转换并统一关键中英文注释。新增 NaN/Inf fail-closed 测试，非法点云几何立即撤销旧名称，
也不能被 OpenCV 提示覆盖。本轮没有改话题、阈值、Gazebo、Nav2 或 Action 接口。

2026-09-01 安全合同复核：视觉确认位现在只在图像类别与有效点云几何一致时为 true；HSV
参数允许 Hue 179→0 环绕，但仍拒绝反向 S/V。T 字台阶最终 Action 闸门已与名称分类统一，
能接受无闪现实测的“低残余高度 + 16.23° 多级踏面趋势”和严格近场 PIT 轮廓，同时拒绝
主坡侧面。缺失、失效或
过期的 NavigationSafety 一律不能触发 Action，异常 Twist 任一分量为 NaN/Inf 时整条归零。
本轮无闪现八障碍固定视角共 317/317 正确命名、导航健康 100%；动态 T 台进入 READY、发布
入口到达并等待真实控制器。0.35 s 几何新鲜窗口已显式归入 autonomous_mission.yaml；
缺失/异常的 Action 结果不会被当成普通成功。该轮全工作区 296 项测试通过；未改 Gazebo、话题、消息或
现有运行参数数值。

2026-09-01 第二轮鲁棒性整理：点云改为选择前向通道内最近且具有足够异常原始回波的
连通域，近杆不再被远墙吞掉，稀疏飞点也不能抢占真实障碍；OpenCV 改用相邻帧运动/尺度
跟踪并发布当前帧框，正常接近、转向和 Image Header 保持一致。冲突视觉不再压低点云
置信度。导航健康增加动态 TF 源时间检查，冻结的 map -> base_link 即使仍在 tf2 缓存中也
会失效。Nav2/TraverseObstacle 的发送、取消和结果都有超时与 generation 隔离；server 未
ready 不消耗补扫/恢复/回访次数，Guidance、TF、odom 和目标中的 NaN/Inf 均 fail-closed。
通信超时会锁存 ACTION_COMMUNICATION_FAULT 和自主 Twist；Nav2 故障可处理 Nav2/核心栈，
Traverse 故障必须处理外部越障控制器，归属不明时两者都处理并使用硬件急停。单独重启自主
launch 或只重启核心栈不安全。

本轮隔离 ROS 域在线复验包含两条互不冒充的证据链：纯场地、无传送的 1 轮矩形轨迹中，
`/navigation/healthy` 测量期 252/252 为 true，map 起终误差 0.0047 m/0.0168 rad；完整白名单
核心进程峰值约 1228 MiB、2.29 个 CPU 核。terrain Header age p95=0.34 s，fused p95=0.44 s，
后者超过 0.35 s 软预算，已如实保留为真机/RK3588 复测项，不影响该工具的安全通过结论。
独立传送替身流程 48 秒完成 8/8、pending=0、回到起点并进入 COMPLETED；Ctrl-C 后最终
`/cmd_vel` 为零。传送结果只验证任务编排，不代表物理越障；全工作区 378 项测试均通过。

2026-08-31 真机标定说明整理：OpenCV、点云、融合和安全评估代码顶部统一为“职责—标定
入口—安全边界”，vision/terrain/terrain_navigation 三个 YAML 顶部统一为“前提—顺序—
问题速查”。完整 rosbag 采集矩阵、CSV 标签、离线重算命令、参数归属和首轮验收线集中在
instruction.txt 第五节。评估器只自动建议视觉置信度和三个高度等级；HSV、横杆间距及点云
几何必须播放原始数据逐组重算，旧预测不能冒充新参数结果。

2026-08-31 核心感知顺序与横杆识别加固：相机、点云及融合安全输入均要求同一会话内
Header 时间严格递增，DDS 重复包/驱动乱序包不再刷新心跳或冒充连续确认；rosbag/Gazebo
时钟回拨会清除旧视觉投票、地面先验和安全迟滞。蓝白限高杆新增蓝段尺寸与间距规律性门，
用于排除同排蓝色杂物。该修改只涉及核心感知/安全消费端，没有修改 Gazebo 或传送替身。

2026-08-28 真机调参可读性整理：根目录新增 AGENTS.md，供另一台电脑的 Codex/代码助手
自动了解当前范围、Gazebo/SLAM/自主任务独立边界、参数归属、注释规则和验证命令。SLAM、
Nav2、传感器 profile、OpenCV、点云、安全引导、自主任务共七个 YAML 的顶部已增加
“现象 -> 参数 -> 调整方向 -> 风险”索引；核心 Python docstring 指回这些唯一参数源，
避免在代码/launch 中产生第二套失配默认值。接入真机先读对应 YAML 第一屏。

2026-08-14 三命令回归还验证：RGB-D 有效深度点约占 75%，RViz 以 RELIABLE QoS 收到标注
图；自动导航 15 秒里程计位移 2.01 m；第三条 Ctrl-C 后 6 秒非零仿真速度为 0、里程计
漂移为 0；/cmd_vel_teleop 手动通道可驱动原地旋转。

2026-08-14 障碍分类复测还验证：T 字台阶不再误报坑；砂砾坑约 0.09 m 深；限高杆点云
净空约 0.31 m 且 OpenCV 可合并蓝白短段；直角杆点云宽约 0.08 m；主坡约 10°；木桥 B
桥板间隙得到专名。该结果来自传感器数据，不读取 Gazebo 模型名或 world 坐标。

2026-08-25 短流程回归选测直角绕杆、砂砾坑、高墙和 T 字台阶：障碍名称增加 3 帧确认
与 4 帧清除迟滞；识别唯一障碍后先原地对准、再接近。斜看高墙时先修正约 14° 后完成
交接，直角绕杆先修正约 11°。同时修复歧义换视角目标被提前取消和近入口小距离/小角度
无命令死区；该结果仍只代表 Gazebo 上层流程，不代表真机腿部能力。

2026-08-26 可移植接口回归：新增“硬件输入/完整算法链路”两级只读检查，已在独立无界面
Gazebo + slam.launch.py 上验证 LaserScan、Odometry、Image、PointCloud2 实时数据、全部传感器
TF、map 定位 TF、强类型算法输出和 /cmd_vel 消费者。检查器会容忍有界的 DDS discovery
冷启动时间，但数据、类型或 TF 真正缺失仍返回非零；未接真实越障 Action 时只明确警告。

2026-08-26 逐障碍账本短流程回归：三进程实跑完成直角绕杆区，先主动修正约 11.2°，再
完成入口与 Action；清单从 0/8 更新为 1/8，并由 transient-local 话题向晚启动终端返回
“已完成直角绕杆区、其余 7 项待完成”。随后识别砂砾坑并进入 SEEKING_PENDING_OBSTACLE，
回到安全观察位重新面向它，同时继续前沿/覆盖探索，证明已知待完成目标不会被遗忘。

2026-08-27 越障成功合同回归：三进程实跑中，直角绕杆 Action 返回后任务先进入
VERIFYING_TRAVERSAL_RESULT，用独立 map TF 核对跨过入口与落地稳定，约 0.9 s 后才将
清单从 0/8 更新为 1/8，随后继续前沿探索。全工作区 228 项测试同步通过。

2026-08-27 参数与节点回归：感知/规划节点在建立通信前统一拒绝非法 ROI、HSV、频率、同步
窗口、高度门限、入口关系、投票窗口、非绝对话题和 `/cmd_vel` 自反馈配置；合法默认配置均
完成真实节点构造。DDS 实测覆盖相机—点云同步融合、相机断流后的纯点云降级以及速度命令
超时归零。全工作区当前增至 245 项测试，核心 Python 目标语句覆盖率约 61%。

2026-08-27 SLAM 健康链回归：readiness/health 节点新增 frame、绝对话题、生命周期服务、
超时、扫描有效率/样本数/视场和里程计跳变参数合同；`nav2.yaml` 显式固定 Nav2 生命周期服务
接口。真实 DDS 连续 10 轮验证有效 LaserScan、Odometry 和 TF 到齐后健康状态为 true，停止
传感器输入后按超时回到 false，测试不依赖 Gazebo；全工作区当前为 252 项测试。

2026-08-27 感知源故障注入：相机/点云候选话题改为“有效消息先取得所有权”。空图、坏编码、
空 frame、零/过期时间戳、截断缓冲区、缺失或越界 XYZ 字段均不会刷新 active source，健康备用
相机或深度点云可以接管；CvBridge 转换失败也会释放当前相机源。该规则保持默认话题兼容性，
不需要绑定具体相机或雷达型号；全工作区当前为 256 项测试。

2026-08-28 自主任务防卡死回归：自主任务终端每 5 秒及清单改变时直接显示已越过/未越过
障碍；Nav2 目标连续 5 秒无位姿进展即取消重规划，越障控制器缺失也只等待 5 秒。无前沿、
覆盖目标或可回访待办时最多补扫两圈，之后携带当前账本返回启动点。三进程实跑中完成
直角绕杆区后清单由 0/8 更新为 1/8，后续前沿停滞约 5 秒即进入 RECOVERY 并继续选目标；
全工作区当前为 260 项测试。

2026-08-28 障碍入口循环复测：限高杆近场点云会在 BAR/WALL/STEP 间变化，旧任务层因此
丢掉中距离已确认语义，并在同一观察点反复 approach。现在接近目标冻结已确认 ID，5 秒无
位姿进展后用实时 map 空间一致性、粗类型、距离、横偏和航向复核；2.35 m 安全包络内直接
交给 TraverseObstacle，守卫不通过则冷却该障碍并换目标。Gazebo 尺寸/碰撞体检查正常，
本次问题属于任务交接而非场地缺口。随后三进程联合复测连续完成“直角绕杆区”和“限高杆”，
清单正确更新为 2/8；通用 STEP/WALL 没有唯一语义时也会按 map 位置冷却，不能原地无限重发。
全工作区 261 项测试记录、0 错误、0 失败。推荐传感器规格已写入 README 4.1 和 instruction 第六节。

2026-08-28 五分钟任务预算优化：最近一次用户联合运行在约 33 s、107 s、180 s 完成直角
绕杆、限高杆和木桥 B，随后砂石木坑的远场专名在入口处退化为通用 STEP，并与约 7.8 m 外
的高墙待办记录反复切换；日志累计 43 次待办回访和 42 次取消，超过 9 分钟仍停在 3/8。
任务层现用“实时 map 位置 + 粗 Action 类型”把近场轮廓重新关联到已多帧确认的同一待办，
回访失败按 8/16/32/64 s 退避。默认总预算改为 300 s，约第 240 s 开始返程并保留 60 s；
终端与 /autonomy/progress 显示已用/剩余秒数。Gazebo 无腿 Action 替身独立采用 0.75 时长
系数，八项模拟动作由约 116 s 缩至约 87 s，不影响 SLAM、Nav2、真机接口或真实步态判据。
本次测试用 ROS_DOMAIN_ID=97 与用户仿真隔离，避免测试节点污染正在运行的任务；全工作区
263 项测试记录、0 错误、0 失败。

同日三进程 300 秒联合复测进一步发现：第一版预算逻辑能在第 240 秒准确进入返程，但只完成
3/8，且旧 T 台观察位在 Nav2 成功到达后错误清零退避；木桥 B 仿真后端因入口方向无法安全
落在场内而拒绝 Action 后，也会被持续画面约每 12 秒重新触发。现在每次回访无论路径成败都
递增 8/16/32/64 s 调度退避，Action 拒绝同样写入该退避，live 感知也必须尊重冷却；控制器
拒绝原因会直接附在任务事件中。返程停滞的直接原因是木桥前地形限速为 0，而 DWB 输出约
0.10 m/s + 0.20 rad/s，旧速度门只接受不超过 0.02 m/s 的“纯旋转”。默认容差改为
0.12 m/s，但门后仍强制 linear=0、只保留 yaw，并继续经过健康/超时/急停守卫。
第二轮复测确认 240 s 返程切换仍准确，但当 Nav2 自身因局部无有效轨迹输出零速时，任务层
每 5 s 取消 return_home，早于行为树恢复。现仅将返程无进展窗口设为 20 s，让 Spin/BackUp/
重规划有执行机会；探索、入口和覆盖目标继续保持用户要求的 5 s 快速切换。

同日第三轮隔离三进程联调：26 s 完成直角绕杆、98 s 完成限高杆、125 s 完成高墙、220 s
完成 T 字形台阶，第 240 s 按预算开始返航，清单为 4/8。该结果仍未达到“五分钟八项并
回原点”，不能作为完成证明。日志新增两个确定根因：长结构最近前缘随纯旋转漂移超过
1.5 m，使匿名目标的换视角计数多次从 1/4 重启；当时的四次验证配置改为按机器人驻留点
计数，并在耗尽后冷却本轮看到的全部前缘。当前版本进一步缩短为两次附加视角（1/2），
参数以 autonomous_mission.yaml 为准。返航时实测 `/cmd_vel_smoothed` 为约 0.40 m/s、-0.61 rad/s，
`/terrain/speed_limit=0`、最终 `/cmd_vel=0`；20 s 超时只能延后取消，不能产生脱困旋转。
现新增 4 Hz `/navigation/rotation_recovery` 心跳，速度门仅在自主任务持有 Nav2 转向的
返航、探索、覆盖、回访、接近、对正和换视角阶段，且心跳 0.8 s 内新鲜时提取最大
0.30 rad/s 纯 yaw，强制所有 linear=0，并保留健康、急停和 Ctrl-C
守卫。该最新两项修复已通过确定性测试，但尚未再次跑完整 300 s 在线全场，后续验收仍以
“八项全部进入已越过且 returned_home=true、总用时不超过 300 s”为唯一通过标准。
隔离 ROS 话题级回归复现同一输入后，心跳有效时最终输出为 linear=0、yaw=-0.30 rad/s；
停止心跳并等待 0.8 s 后最终输出恢复全零。该轮全工作区为 264 项测试、0 错误、0 失败。

同日最新联合诊断与修复：一次完整 300 秒运行在约 240 秒完成 4/8，但返程没有产生有效
位移；加入主动换站后，正常起点复测在约 118 秒完成高墙、167 秒完成砂砾碎木坑，240 秒
共完成 3/8，至 300 秒仍未回原点，因此尚未达到“五分钟八项并回原点”。本轮同时在长障碍
附近复现“通用踏板语义 + 前沿目标每 5 秒取消”的循环。任务现在把 5 秒有效进展定义为
至少 0.04 m 平移或 0.06 rad 转动；前沿确由障碍阻挡、入口守卫不通过或多视角仍有歧义时，
会交替转向 90°，再用普通 Nav2 安全链尝试移动 0.80 m，真正改变观察站。已完成的唯一比赛
专名在最终 Action 交接前还会再次拦截，避免别处误报导致重复越障；锁定名称只有在新名称
连续三帧且与当前米制几何一致时才替换。未知木桥专名不能触发 Action，因为实跑已证明
主坡侧面也可能短暂产生该轮廓。该轮自动回归为 272 项、0 错误、0 失败；主动换站修改
返程日志还证明 Nav2 会请求 0.50 m/s 直穿眼前台阶，而安全门正确输出零；返程 20 秒停滞
现在同样先排队有界转向和普通 Nav2 换站，且换站目标不会被返程状态立即取消。该最后一项
在当时尚未再次通过完整 300 秒实跑，所以该历史轮次不能宣称满足 5 分钟。验收仍只看 8/8、
returned_home=true、elapsed_seconds<=300 三项同时成立。

2026-08-31 仿真边界整理：无动力学测试狗不再逐帧“穿过”碰撞体。完整流程第一条改用
`ros2 launch quadruped_gazebo robocon_field_teleport.launch.py`，任务层完成实时识别和对正后，
Gazebo 替身只调用一次 SetEntityPose 将模型传送到出口并返回 Action 成功。纯传感器/SLAM
测试仍用 `robocon_field.launch.py`。`autonomous_navigation.launch.py` 已完全移除 Gazebo 后端
选择和启动代码，真机只需提供同名 `/traverse_obstacle` 服务端。

同日三分钟流程验收使用 Gazebo 闪现、slam_sim 和 autonomous_navigation 三个独立进程：
48 秒完成八项，completed=8/8、pending=0/8，模型回到起点，任务进入 COMPLETED，得分 1300。
闪现入口以标准 `/perception/fused_obstacle` 提供确定性仿真样本，并只在核心清单实际新增后
切换下一观察位；它不发布完成结果。该成绩验证上层流程与时限，不代表原始 OpenCV/点云识别、
SLAM 回环或真机步态成绩。真实传感器准确率仍使用纯 `robocon_field.launch.py` 测试。
该次历史提交的基线为 288 项自动测试通过；当前数量见本文件顶部。

未完成且暂不开发：真机动力学仿真、厂家 SDK、ros2_control、状态估计、FK/IK、动力学、
站立步态、关节控制、真实越障 Action 服务端和正式比赛流程。当前完成的是 Nav2 到障碍
入口、Action 客户端/合同和后续探索；没有真实服务端时不会抬腿或完成真实跨越。当前 Gazebo
仅用于 SLAM/Nav2/OpenCV 的场景与接口测试，不能代表四足越障能力。

越障完成采用双层判据：真机 Action 服务端只有在目标动作完整结束、无取消/超时/故障、
足端接触与机身姿态闭环确认稳定落地后才能返回 success=true；自主任务随后还要核对 ROS
Action 为 SUCCEEDED、机器人已越过在线观测入口且连续稳定 0.75 s。任何一项失败都不从
待完成清单删除。八项分别通过并最终到达终点后，才算整场自主任务完成。

技术基线：后续强化学习新工程统一使用 Isaac Lab（Isaac Sim 上层框架）。训练放在独立
NVIDIA GPU 工作站，RK3588 运行 ROS 2 与部署后的策略。当前 Nav2 全局规划继续使用官方
NavFn；D* Lite 不是 Nav2 Jazzy 内置插件且尚无性能
证据支持自研替换。真机参数确定后优先对比官方 SmacPlanner2D/SmacPlannerLattice。

二、新电脑完整复现
----------------
系统：Ubuntu 24.04 amd64/arm64；ROS 2 Jazzy Desktop。先按 ROS 官方说明配置 apt 源，
再执行：

  sudo apt update
  sudo apt install -y git build-essential python3-pip python3-rosdep \
    python3-colcon-common-extensions python3-opencv \
    ros-jazzy-desktop ros-jazzy-slam-toolbox ros-jazzy-navigation2 \
    ros-jazzy-nav2-bringup ros-jazzy-cv-bridge ros-jazzy-tf2-sensor-msgs \
    ros-jazzy-pointcloud-to-laserscan ros-jazzy-robot-state-publisher \
    ros-jazzy-xacro ros-jazzy-rviz2 ros-jazzy-rosbag2 ros-jazzy-joy \
    ros-jazzy-ros-gz

首次使用 rosdep：

  sudo rosdep init        # 如果提示 already initialized 可忽略
  rosdep update

获取并构建：

  git clone <仓库地址> ~/wakula
  cd ~/wakula
  source /opt/ros/jazzy/setup.bash
  rosdep install --from-paths src --ignore-src -r -y
  ./scripts/build.sh
  source install/setup.bash

建议把下行加入 ~/.bashrc（路径不同请修改）：

  source /opt/ros/jazzy/setup.bash
  source ~/wakula/install/setup.bash

VS Code：安装 Visual Studio Code 后，打开 ~/wakula。建议 Python、Pylance、C/C++、
CMake Tools、XML、YAML、ROS 插件；项目已有 .vscode/settings.json 和 tasks.json。

三、启动前必须由外部提供
--------------------
最低导航输入：
  /scan      sensor_msgs/msg/LaserScan
  /odom      nav_msgs/msg/Odometry
  odom -> base_link TF

SLAM 产生 map -> odom。启用地形感知还需：
  RGB Image
  PointCloud2（x/y/z，米）
  base_link 到传感器 frame 的 TF

当前仓库不会生成真实里程计或驱动电机。

四、常用启动命令
--------------
正常联调只需三个运行终端、三个命令：

  cd ~/wakula
  source /opt/ros/jazzy/setup.bash
  source install/setup.bash
  # 终端 1：Gazebo 场地/传感器 + 独立的一次性传送越障替身
  ros2 launch quadruped_gazebo robocon_field_teleport.launch.py
  # 终端 2：仅核心 SLAM、Nav2、OpenCV、点云和 RViz；显式锁定仿真时间
  ros2 launch slam slam_sim.launch.py
  # 终端 3：独立自主探索与越障任务；启动即运行
  ros2 launch slam autonomous_navigation.launch.py

第三条不启动或关闭 SLAM、Nav2、OpenCV、RViz、Gazebo，也不启动仿真越障后端；现实传感器和 Gazebo 传感器接口
一致。在第三个终端按 Ctrl-C，会先令 Nav2 Twist 归零，再请求取消 Action；第一、二条
进程继续运行。Gazebo 替身或真机控制器确认释放所有权后，键盘/手柄仍可人工接管；若真实
TraverseObstacle 失联，必须先硬件急停或停服，不能把自主速度锁当作关节急停。

若终端出现 ACTION_COMMUNICATION_FAULT，表示 Action 响应/取消超时，程序无法证明旧
Nav2 或越障控制器已经释放运动权。此时只保证 Nav2 Twist 被锁；用硬件急停/停服处理可能的
关节运动，再停止或重启日志指明的所有者。Nav2 故障可重启 Nav2/核心栈，Traverse 故障必须
处理外部越障控制器，归属不明时两者都处理；只重启第三条或只重启核心栈不构成安全恢复。

自主任务按“发现 → 多帧确认 → 主动对正 → 入口 → Action → 记录完成 → 下一项”串行执行。
已知但尚未越过的障碍优先回到安全观察位重新识别，未知项继续由前沿和覆盖探索寻找；全部
完成后默认回到启动点作为终点。运行中查看清单：

  ros2 topic echo /autonomy/completed_obstacles
  ros2 topic echo /autonomy/pending_obstacles

第三个终端本身也会每 5 秒和清单变化时打印同样的中文清单。单个 Nav2 目标 5 秒没有
有效平移/旋转进展会自动取消重规划；越障控制器未就绪时最多等待 5 秒。所有可用探索目标
耗尽后会原地分 8 次补扫两圈，仍未发现新目标便带着未完成清单返回启动点。

正式终点不同，由独立赛务模块发布 /autonomy/finish_pose（PoseStamped、map frame）；不需要
修改 Gazebo、SLAM、Nav2 或 OpenCV 源码。

第一条在 GUI 模式下会自动打开 `Wakula Simulation Keyboard` 窗口；点击该窗口后使用
i/k 前后、j/l 原地旋转、任意其他键停车。无需运行第四条键盘命令。

真机要求运动组提供 /traverse_obstacle Action 服务端。仿真三命令模式由第一个 Gazebo
组合入口提供一次性传送 Action；第三个自主任务命令不包含任何仿真代码。只做 SLAM/感知、
不测试任务越障时可改用纯场地入口：

  ros2 launch quadruped_gazebo robocon_field.launch.py

在线探索使用 16 m × 8 m 滚动全局代价地图；任务层只容忍机器人超出最新 SLAM 栅格
边界 0.30 m 以内的短暂发布滞后，超过后仍停在 WAITING_FOR_MAP。该策略避免仿真越障
落点仅越界几厘米就永久停止，同时不会放行定位大跳变。

以下命令只供运动控制开发者脱离自主任务单独验证 Action 合同：

  ros2 launch quadruped_gazebo sim_traversal_controller.launch.py

仿真中普通 slam.launch.py 自动模式的启动摘要必须包含：
  simulation_detected=true, use_sim_time=true, robot_model=false
入口会重试检测 /clock。若现场仍未显示上述值，停止该算法进程并显式运行：
  ros2 launch slam slam_sim.launch.py
该显式入口会先打印 Wakula simulation mode；随后核心摘要的 simulation_detected=false
只表示跳过自动探测，use_sim_time=true、robot_model=false 才是必须检查的生效值。
然后再启动独立自主 launch；仿真传感器时间与算法系统时间不一致时会安全输出零速。

无相机：

  ros2 launch slam slam.launch.py vision:=false

无 RViz：

  ros2 launch slam slam.launch.py rviz:=false

回放 bag：

  ros2 launch slam slam.launch.py use_sim_time:=true
  ros2 bag play <bag目录> --clock

设备 profile：

  ros2 launch slam slam.launch.py sensor_profile:=orbbec_gemini2

自定义话题：

  ros2 launch slam slam.launch.py \
    scan_topic:=/lidar/scan odom_topic:=/localization/odom \
    camera_topic:=/camera/color/image_raw point_cloud_topic:=/camera/depth/points

只启动感知与速度门，不启动 SLAM/Nav2：

  ros2 launch quadruped_bringup bringup.launch.py

Xbox 手柄（使用独立 launch，刻意不加入 slam.launch.py）：

  ros2 launch quadruped_teleop xbox_teleop.launch.py

手柄启动后，十字键上等价于另开终端执行
`ros2 launch slam autonomous_navigation.launch.py`，十字键下等价于在该自主任务终端按
Ctrl-C。自主任务默认不启动；按住不会重复触发。下键只结束由当前 xbox_teleop 创建的任务，
不关闭 Gazebo、核心 slam.launch.py 或其他终端手动启动的自主任务。

Gazebo 比赛参考场地（同样独立，不加入 slam.launch.py）：

  ros2 launch quadruped_gazebo robocon_field.launch.py

完整自主流程使用 Gazebo 组合入口（场地 + 对准后一次传送 Action，仍不含算法）：

  ros2 launch quadruped_gazebo robocon_field_teleport.launch.py

无 GUI：

  ros2 launch quadruped_gazebo robocon_field.launch.py gui:=false

只看场地、不生成传感器载体：

  ros2 launch quadruped_gazebo robocon_field.launch.py spawn_test_robot:=false

联合测试须另开终端启动算法，避免重复发布占位 TF：

  ros2 launch slam slam_sim.launch.py

slam_sim.launch.py 不启动 Gazebo，只把核心 slam.launch.py 固定为 use_sim_time=true、
robot_model=false，避免漏参造成仿真时钟不一致或重复 TF。真机仍使用 slam.launch.py。
普通 slam.launch.py 现在也会自动检测运行中的 /clock；首行必须显示
simulation_detected=true、use_sim_time=true、robot_model=false。
这会由算法入口启动 RViz、SLAM Toolbox、Nav2、OpenCV 和点云分析。通用机械狗测试替身已经发布默认
/scan、/odom、odom->base_link、/imu/data、/camera/image_raw、/camera/depth/points，并
提供 lidar_link、camera_link、camera_optical_frame、imu_link 的 TF，不需要额外 remap。
它的腿仅为固定外观，载体保持水平且无动力学碰撞，只用于算法联调；不能验证步态/接触，
直接 /cmd_vel 也可能穿过障碍。仿真 RGB-D 光心位于机头外并向下俯约 14°，避免自身
外观遮住 RGB 或令深度云全部为 Inf；默认 424×240@15 Hz 并关闭传感器可视化，
world 物理时钟为 100 Hz，/clock、关键导航和辅助数据分桥运行，避免 GUI/RViz 同机时
/scan 与 /odom 断流；默认话题不变，真机更换常见分辨率
无需改算法。正式模型可这样一次替换：

  ros2 launch quadruped_gazebo robocon_field.launch.py \
    robot_sdf:=/绝对路径/real_quadruped/model.sdf \
    robot_name:=real_quadruped publish_test_sensor_tf:=false

联合测试中 Gazebo 的终端/窗口必须保持运行；关闭后 RViz 可能仍保留最后一张地图，但数据
已经停止。出现“感知数据无效”先分别执行 ros2 topic hz /scan、/odom 和
/camera/depth/points。当前参考仿真的正常值约为 15 Hz、25~30 Hz 和 15 Hz；地图在运动中
约 4 Hz 发布，扫描匹配最多约 10 Hz，8 cm / 0.08 rad 即可加入新关键帧。

RViz 的 Camera Detection 面板会显示 /vision/annotated_image：青色为 ROI，黄色为当前帧
候选，绿色为多帧稳定结果。标注图使用与 RViz 默认 Image 显示兼容的 RELIABLE QoS；
算法终端会周期打印“[正前方障碍]”；也可单独查看：

  ros2 topic echo /perception/front_obstacle_name

名称以可量测几何为准：10° 坡面显示“主斜坡”，14° 坡面显示“木桥引坡”，其余可显示
T 字形台阶、限高杆、直角绕杆区、砂砾与碎木坑或高墙。仅检测到比赛颜色而点云尚未完成
分类时，会显示“视觉检测到有色比赛障碍（点云待分类）”；这不是程序漏报，也不会冒充已
确认类别。木桥 B、坑区护栏/填料及限高杆支柱会显示接近阶段名称；算法不会读取仿真坐标，
证据不足以区分结构时会明确显示“待确认”。

当前识别回归基线：宽台阶/木桥不会再因占据多数而把入口反报成坑；蓝白限高杆可由多个
蓝色短段合并，并由点云给出约 0.30 m 净空；约 70 mm 立柱即使只占 1～2 个高度格，也会
在原始回波和三维跨度充足时保留。纯 Canny 大框不再猜成 WALL，墙面必须由点云确认。
若标注图显示 `VISION: NONE` 但 `FRONT` 和终端名称正确，表示 OpenCV 没有可证明的颜色
结构，不是点云漏检；视觉只做辅助，不能为追求“每帧都有名字”而放宽成误报。

RViz 地图：白色=已观测自由区，黑色=障碍，灰色=未知区。默认隐藏 TF、网格和实时
LaserScan；顶视图默认跟随 base_link，地图仍固定在 map 坐标，并以 0.25 s 周期发布。
排查时再手动勾选。开放场地初始的白色射线边缘属于探索范围；低速覆盖通道，
每到转角停留并旋转一周，再通过闭环后的黑色墙线重合程度判断 SLAM。地图整体旋转不影响
导航。若出现从机器人向外发散的黑墙，先检查二维雷达是否水平以及 lidar_link TF。
若终端显示“视觉疑似××（点云未确认）”，表示 OpenCV 只触发保守限速，几何类别尚未
得到点云尺度确认。

Nav2 只会在雷达角度/样本/frame、里程计时间/frame/协方差和定位 TF 全部有效且源时间新鲜后激活。
tf2 缓存里能查到旧变换不代表健康；`/diagnostics` 的 `tf_age_seconds` 持续增大或显示
`unavailable` 时，先修 map -> odom / odom -> base_link 发布者和仿真/系统时钟。
点云进入局部代价地图前会自动剔除拟合地面和坡面；真机只需保证点云 TF 正确，并用
rosbag 调整 terrain.yaml 的相对地面高度阈值，不要在相机驱动侧重复裁成绝对 z>0。
GUI Gazebo 已自动提供键盘窗口。只有用 `keyboard_teleop:=false` 关闭自动窗口或单独排障时
才运行备用脚本：

  cd ~/wakula
  ./scripts/keyboard_teleop.sh

自动长时间联合回归（只允许对仓库自带 Gazebo 测试狗使用，会主动发布速度）：

  ros2 run quadruped_tools stack_regression --allow-motion --cycles 5 \
    --pipeline-latency-budget 0.35 --report reports/stack_regression.json

它在矩形异路闭合与双向整圈旋转/前进/倒退之间交替，并验证 Nav2 多目标、1 m 绕杆窄通道、
不可达目标恢复；记录 map/odom 起终一致性、数据间隔、错误 READY、ALIGN/READY 边界抖动、
明确白名单内所有核心进程 CPU/RSS，以及 terrain/fused Header age 的 p50/p95/max。每个请求
周期缺 map TF，或 map 起终指标出现 NaN/Inf，都不能通过；odom 指标仅作漂移诊断。测量窗口
内任一 navigation/healthy=false 也不能通过，任一数据流末帧过旧同样按断流失败。
默认使用 Gazebo /clock；系统时间数据源加 --no-use-sim-time。延迟预算是跨机器软告警，
不改变 passed。closed_path_pose_consistency 只表示轨迹结束后的位姿一致性，报告固定写明
不能证明 SLAM Toolbox 回环图优化；真正回环需用带 odom 漂移的 bag 做开/关回环 A/B。
2026-08-13 入口稳定性复测收到 665 帧 guidance 且错误 READY 为 0；另一次
边界复测进入 READY 6 帧，无小于 0.35 s 的 ALIGN/READY 快速往返。未写 --allow-motion 会
拒绝运行；真机不得用这个命令代替正式测试流程。

保持键盘终端获得焦点，i/k 前后、j/l 原地旋转。不要再把键盘直接 remap 到 /cmd_vel：
自动导航最终速度门也会发布该标准话题，二者会互相覆盖。Gazebo 专用 mux 将
/cmd_vel_teleop 作为短时高优先级输入，统一输出 /cmd_vel_gazebo；真机接口不受影响。

若日志持续出现 Detected jump back in time，先运行 `ros2 topic info /clock --verbose`；通常
是重复启动了两个 Gazebo。每次只能保留一个 robocon_field.launch.py，停止旧实例后重启
Gazebo 和算法。当前 launch 已隔离 Snap VS Code 的 GTK_PATH，`libpthread/GLIBC_PRIVATE`
不应再出现；若使用自定义 launch，仍需清除该变量或改用 Ubuntu 系统终端。
Ubuntu 崩溃窗口可能延迟显示旧报告；核对窗口中的 Date 和最新 ~/.ros/log。最新日志若显示
“rviz2 process has finished cleanly”，可关闭旧弹窗，不要误判为当前算法崩溃。
检查 TF 优先运行 `./scripts/diagnose.sh`。不要使用 `tf2_echo ... | head`：tf2_echo 会持续
输出，head 提前关闭管道时 ROS 2 Jazzy 可能弹出 BrokenPipeError；这不是 SLAM/Nav2 崩溃。
若曾看到 collision_monitor 在 get_subscription_count() 处 SIGSEGV，那是旧版本速度链。
当前版本不再启动该 Jazzy 进程：navigation_speed_gate 直接输出 /cmd_vel，并用 /scan 做
0.22 m 极近距离急停。重新编译并用 slam.launch.py 启动即可。

规则只公布障碍结构尺寸与颜色，尚未公布正式排列坐标。参考 pose 全部集中在
src/quadruped_gazebo/worlds/robocon_obstacle_field.sdf 开头的 REFERENCE LAYOUT；正式坐标
到手后只修改八个 layout_* frame，不修改算法 launch 或参数。

先用 `ros2 topic echo /joy` 核对实际轴/按键下标，再查看 `/cmd_vel_joy`。默认不会接管
`/cmd_vel`；真机阶段增加 twist_mux，将手柄和 Nav2 仲裁结果送入硬件安全层与驱动。
LB 必须在摇杆回中时按下才会解锁；带量按下会被拒绝，需松开并重新按下。轴方向相反时
修改 xbox.yaml 的 linear_x_direction、linear_y_direction 或 angular_z_direction 为 -1.0。
十字键上下默认读取 axes[7]（上 +1、下 -1）；若驱动相反，将 dpad_y_direction 改为 -1.0。
用 `ros2 topic echo /teleop/autonomy_process` 可查看 STOPPED/RUNNING/STOPPING/FAILED。
若手柄断流，节点持续输出零速度；恢复连接后必须先松开并重新按下 LB。

五、快速验收
----------
  ros2 topic hz /scan
  ros2 topic hz /odom
  ros2 topic hz /terrain/features_stamped
  ros2 topic hz /vision/obstacle_stamped
  ros2 topic hz /vision/annotated_image
  ros2 topic echo /perception/front_obstacle_name
  ros2 topic echo /terrain/navigation_mode
  ros2 topic echo /terrain/speed_limit
  ros2 topic echo /terrain/navigation_safety
  ros2 topic echo /traversal/guidance
  ros2 topic echo /traversal/phase
  ros2 run tf2_ros tf2_echo base_link <点云frame>
  ros2 run tf2_ros tf2_echo map base_link
  ./scripts/diagnose.sh

需要定位某个话题由谁发布/订阅时，先查 connect.txt 第二节的节点作用与关键接口，再运行：

  ros2 node list
  ros2 node info /节点名
  ros2 topic info /话题名 --verbose

正常现象：vision=true 时相机短暂缺帧会先等待同步，超过 0.25 s 后自动使用纯点云结果；
点云也缺失或无效才保持 speed_limit=0。识别 CLEAR 且数据持续新鲜后为 1；
越障目标在远处先低速 APPROACH/ALIGN，进入 1.20 m 交接区后为 READY 且速度归零；
普通/矮 POLE 由 Nav2 低速绕行，只有高度≥0.45 m 且语义确认为直角绕杆赛项时才进入 Action。
相机证据不能触发腿部动作。`/navigation/healthy` 为 false 或
断流时速度门也保持零。只有点云时
使用 vision:=false，决策会自动采用兼容路径。

六、录包与离线评估
----------------
  ./scripts/record_bag.sh
  ros2 bag info bags/<bag名>
  ros2 run quadruped_tools perception_bag_evaluator --help

完整操作只看 instruction.txt 第五节，避免在本速查文件维护第二套易失配步骤。
每种障碍至少采集不同距离、角度、速度、明暗、逆光和背景；还要采集大量无障碍负样本。
视觉集必须额外覆盖全黑/全白、局部阴影、顶灯频闪、金属高光、镜头污渍、运动模糊和
部分遮挡；点云集覆盖黑色/反光表面、深度空洞、孤立及相邻飞点、坡面和砂砾。
标签必须对应传感器时间，不要按观看视频的电脑时间粗略对齐。优先降低坑洞/高墙漏检，
再优化普通类别误报。

七、开发与提交检查
----------------
  ./scripts/build.sh
  source install/setup.bash
  colcon test
  colcon test-result --verbose
  git status --short
  git diff --check

修改算法时同步更新 README.md、instruction.txt、connect.txt、quickstart.txt。新增通信接口先
更新 connect.txt；改变参数或流程更新 instruction.txt；新增启动方式更新 quickstart.txt。
新增模块或修改关键算法时同步维护模块/函数 docstring、参数分组、单位、坐标系、失效行为和
“为何采用该策略”的注释；不要用逐行复述代码的注释制造阅读噪声。
Codex 或其他代码助手还必须先读取根目录 AGENTS.md；部署调参只修改其归属 YAML。Python
参数声明可保留与 YAML 同步且有测试的启动回退，README 数字只是版本快照；不得把它们变成
第二调参入口或另建临时 YAML。提交前确认实际 launch 正在加载被修改的参数文件。

八、真机出来后的接入顺序
--------------------
1. 测量传感器安装位姿并修正 URDF/静态 TF。
2. 启动传感器驱动，通过 profile/remap 对齐 Image、PointCloud2 和 LaserScan。
3. 接入可靠 `/odom` 与 odom->base_link，只验证 SLAM 和 Nav2，不连接电机速度。
4. 录真实 bag，重调点云与 OpenCV，完成准确率统计。
5. 单独开发硬件驱动、状态估计、运动学、步态和安全层，并在台架验证。
6. 底盘能安全消费 `/cmd_vel` 后低速接入 Nav2。
7. 实现现有 /traverse_obstacle Action 的真机服务端与安全控制权切换；不得把收到 Goal
   或动作序列播放完当作越障完成。只有无取消/超时/驱动故障、预期足端接触成立且姿态
   闭环确认稳定落地后才能返回 success=true；任务层还会独立验证越过入口与落地静止。

九、其他团队一键对齐
------------------
硬件/状态估计组交付以下标准接口即可：/scan（LaserScan）、/odom（Odometry）、
odom->base_link、传感器 TF、Image 和 PointCloud2。底盘/运动控制组订阅最终 /cmd_vel
（Twist）；越障控制组实现 /traverse_obstacle 服务端，并以 /terrain/navigation_safety
复核环境有效性。不要把 STEP/CLIMB 或 READY 直接转换成关节动作；READY 只表示入口距离
和对正条件满足，Action Result 才向任务层表达完成/失败。

真机发布自身 URDF/TF 时这样启动：

  ros2 launch slam slam.launch.py robot_model:=false sensor_profile:=ros_default

先在算法未启动时验收硬件输入（真实消息、frame_id 与传感器 TF）：

  ./scripts/check_integration.sh --inputs-only \
    --image /camera/image_raw --points /camera/depth/points

全栈稳定后再执行完整只读验收：

  ./scripts/check_integration.sh \
    --image /camera/image_raw --points /camera/depth/points

实际话题不同就替换具名参数，或先在 sensor_profiles.yaml 添加设备 profile。profile/remap
必须使用 / 开头的绝对话题。完整检查还验证 map/odom TF、算法输出和 /cmd_vel 消费者；
真机越障服务端就绪后增加 --require-traverse-server。失败项按 connect.txt 修正。脚本不发送
速度，也不启动硬件。VS Code 命令面板可直接运行同名 ROS 对接检查任务。

十、RK3588 建议
-------------
保持 OpenCV 路径，不启用 YOLO；当前在线档为图像最大宽度 576、处理 5 Hz，点云处理 5 Hz。
独立 Gazebo 测试载体使用 424×240，但真机可先用 640×480 或 640×360，再根据实测余量调整。
确认 CPU/内存余量后再提高分辨率或频率。优先让相机/雷达驱动在硬件侧完成对齐、降采样
和滤波，避免同一份点云被多个节点重复转换。算法侧 `terrain.yaml` 的
transform_max_points 默认把 TF 前原始云限制为 40000 点，几何分析再限制为 12000 点，
Nav2 障碍云最多 1800 点；离线对照时可设为 0，真机算力
不足时应结合 rosbag 准确率逐步降低，不能只凭运行流畅程度调参。
高度栅格分位数已向量化；本机 50 次、每次 20000 点的纯几何基准由约 4.63 s 降到
0.30 s。2026-09-01 按完整 executable 白名单、关闭 RViz 的 x86+Gazebo 回归中，核心进程
峰值约 1228 MiB、瞬时约 2.29 个 CPU 核；该口径排除 Gazebo/RViz，但 RK3588
仍须单独记录温度、功耗、降频和端到端延迟。无显示器部署可令 vision.yaml 的
publish_annotated_image=false，减少标注图复制和编码；不要为了省算力关闭安全数据链。

十一、移植到已有机器人仓库速查
--------------------------
本次目标机器人没有 SLAM/Nav2，固定采用完整移植：复制 interfaces、description、perception、
planning、bringup、slam 六个源码包，缺少任何一个都不算完整算法。不要复制
build/install/log、Gazebo、Xbox 或 tools。源码放入目标工作空间 `src/` 后运行 rosdep，先
单独编译 quadruped_interfaces，再用 `--packages-up-to slam` 构建其余包。
下文若要直接运行 `./scripts/check_integration.sh` 或录包，还需从 Wakula 根目录复制
`scripts/check_integration.sh`、`scripts/record_bag.sh` 到目标工作空间 `scripts/`；这两份
是开发辅助脚本，不属于六个运行期 ROS 包。

启动目标机器人的传感器、里程计和 TF 后，使用完整算法入口：

  ros2 launch slam slam.launch.py robot_model:=false \
    scan_topic:=/实际scan odom_topic:=/实际odom \
    camera_topic:=/实际image point_cloud_topic:=/实际points

该命令由本项目启动 SLAM、Nav2、OpenCV、点云、融合和安全决策。目标已有底盘速度仲裁器时
追加 `speed_gate:=false`；此时 Wakula 不发布 `/cmd_vel`，目标仲裁器必须订阅
`/cmd_vel_smoothed` 作为自动候选，并同时消费 `/terrain/navigation_safety`、
`/navigation/healthy`、`/navigation/autonomy_stop` 后唯一发布 `/cmd_vel`。autonomy_stop
只封锁自动候选，不封锁人工分支；这不会关闭本项目 SLAM/Nav2。
自主越障另需真机运动控制提供 `/traverse_obstacle` 服务端。详细复制、构建、启动和验收步骤
见 instruction.txt 第十节；接口所有权表见 connect.txt 第八节。

真机首次调试不要直接运行自主任务。最短安全顺序是：

  # 1. 电机禁用，只检查硬件消息与 TF
  ./scripts/check_integration.sh --inputs-only \
    --scan /实际scan --odom /实际odom --image /实际image --points /实际points

  # 2. 被动检查感知与建图，不启 Nav2，不发布最终速度
  ros2 launch slam slam.launch.py use_sim_time:=false robot_model:=false \
    nav2_enabled:=false speed_gate:=false \
    scan_topic:=/实际scan odom_topic:=/实际odom \
    camera_topic:=/实际image point_cloud_topic:=/实际points

  # 3. SLAM、识别和 rosbag 验收后再启 Nav2，但仍不把速度送进电机
  ros2 launch slam slam.launch.py use_sim_time:=false robot_model:=false \
    speed_gate:=false scan_topic:=/实际scan odom_topic:=/实际odom \
    camera_topic:=/实际image point_cloud_topic:=/实际points

确认 `/navigation/healthy=true`、地图/TF/障碍结果稳定且 `/cmd_vel` 所有权唯一后，才进入低速
空场、单障碍和自主任务测试。详细通过标准、故障注入和测试记录要求见 instruction.txt
第十节 6.1～6.8。
